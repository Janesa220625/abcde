// This file is kept as a placeholder to prevent import errors
// All Firebase Admin SDK functionality has been removed as the application now uses Supabase

/**
 * Placeholder function that returns a not-available status
 * @deprecated Firebase is no longer used. Use Supabase instead.
 */
export const initializeFirebaseAdmin = () => {
  console.warn("Firebase Admin SDK is no longer used. Use Supabase instead.");
  return null;
};

/**
 * Placeholder function that returns a not-available status
 * @deprecated Firebase is no longer used. Use Supabase instead.
 */
export const getAdminStorage = () => {
  console.warn("Firebase Admin SDK is no longer used. Use Supabase instead.");
  return null;
};

/**
 * Placeholder function that returns a not-available status
 * @deprecated Firebase is no longer used. Use Supabase instead.
 */
export const checkBucketExists = async () => {
  console.warn("Firebase Admin SDK is no longer used. Use Supabase instead.");
  return false;
};

/**
 * Placeholder function that returns a not-available status
 * @deprecated Firebase is no longer used. Use Supabase instead.
 */
export const createStorageBucket = async () => {
  console.warn("Firebase Admin SDK is no longer used. Use Supabase instead.");
  return {
    success: false,
    error:
      "Firebase has been removed from this application. Use Supabase instead.",
  };
};
